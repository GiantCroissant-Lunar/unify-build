plugins {
    id 'com.android.application'
}

android {
    namespace 'com.example.unifysample'
    compileSdk 34

    defaultConfig {
        applicationId "com.example.unifysample"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
        debug {
            // default debug config
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
