# For more information about the Appfile, see:
# https://docs.fastlane.tools/advanced/#appfile

package_name(ENV["ANDROID_PACKAGE_NAME"] || "com.example.yourapp")
json_key_file(ENV["GOOGLE_PLAY_JSON_KEY"] || "path/to/google-play-key.json")
