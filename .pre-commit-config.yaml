repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: mixed-line-ending

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: dotnet-format
        name: dotnet format
        # Workspace path is required when running from repo root since there's no .sln here;
        # the solution lives at dotnet/UnifyBuild.sln.
        entry: dotnet format dotnet/UnifyBuild.sln --verify-no-changes
        language: system
        types: [c#]
        pass_filenames: false
      - id: repomix
        name: Generate repomix output
        entry: cmd /c npm run repomix
        language: system
        pass_filenames: false
        always_run: false
