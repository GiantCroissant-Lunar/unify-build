# Claude Code compatibility target.
#
# Claude Code reads CLAUDE.md as its project instruction file. Keep AGENTS.md
# as the tracked canonical source and generate only this thin import shim.

tool: claude
display_name: Claude Code

rules:
  strategy: import
  target: CLAUDE.md
  source: AGENTS.md

mcp:
  enabled: false

hooks:
  enabled: false
