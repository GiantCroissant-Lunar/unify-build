workflow: GitHubFlow/v1
mode: ContinuousDeployment
next-version: 0.1.0
strategies:
- ConfiguredNextVersion
- Mainline
- TaggedCommit
branches:
  main:
    regex: ^main$
    increment: Patch
    label: ''
