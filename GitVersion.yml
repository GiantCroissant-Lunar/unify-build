workflow: GitHubFlow/v1
mode: ContinuousDeployment
next-version: 0.3.2
strategies:
- ConfiguredNextVersion
- Mainline
- TaggedCommit
branches:
  main:
    regex: ^main$
    increment: Patch
    label: ''
