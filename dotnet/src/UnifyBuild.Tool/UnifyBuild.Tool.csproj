<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <PackAsTool>true</PackAsTool>
    <ToolCommandName>unify-build</ToolCommandName>
    <PackageId>UnifyBuild.Tool</PackageId>
    <Authors>plate</Authors>
    <Description>CLI tool for running UnifyBuild targets from build.config.json without a Nuke build project.</Description>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageProjectUrl>https://github.com/GiantCroissant-Lunar/unify-build</PackageProjectUrl>
    <RepositoryUrl>https://github.com/GiantCroissant-Lunar/unify-build</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageTags>build;nuke;dotnet;unity;ci;cd</PackageTags>

    <NukeTelemetryVersion>1</NukeTelemetryVersion>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\UnifyBuild.Nuke\UnifyBuild.Nuke.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Include="README.md"
          Pack="true"
          PackagePath="" />
  </ItemGroup>

</Project>
